version: '3.8'
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: code
      POSTGRES_PASSWORD: codepassword
      POSTGRES_DB: nestdb
    ports:
      - 5499:5432